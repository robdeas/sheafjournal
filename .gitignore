# --- SvelteKit ---
.svelte-kit
/build
/package
.env
.env.*
!.env.example

# --- Bun ---
bun.lockb
node_modules
# If you use bun's install cache
.bun-cache

# --- JetBrains (WebStorm) ---
.idea/
*.iws
*.iml
*.ipr
cmake-build-*/
out/
# Ignore the local workspace file if you don't want to sync window layouts
.idea/workspace.xml

# --- Build / Output ---
dist/
/out/
/.vercel
/.netlify
/.adapter-pkg

# --- Logs & OS ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db

# --- Testing ---
/tests-results
/playwright-report
/test-results

# --- SheafJournal Binaries ---
*.exe
sheafjournal
dist/
dist-linux/

# --- Build/Dev Artifacts ---
# We can ignore the .env files because the Makefile generates them fresh
# and the Go binary bakes the UUID in via -ldflags.
# sheaf-launcher.env
# sheafgate-launcher-engine.env
bun.zip


# --- Sub-Project Build Folders ---
# Matches SvelteKit/Quarkus build outputs
**/build/
**/.svelte-kit/
**/node_modules/
**/target/